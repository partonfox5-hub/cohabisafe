<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amenities - CohabiSafe</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .amenity-grid { display: grid; gap: 15px; }
        .amenity-card { background: white; padding: 15px; border-radius: 8px; border: 1px solid #eee; display: flex; justify-content: space-between; align-items: center; }
        .preference-select { padding: 8px; border-radius: 4px; border: 1px solid #ccc; }
        .preference-select.must { border-color: #2ECC71; color: #2ECC71; font-weight: bold; }
        .preference-select.no { border-color: #e74c3c; color: #e74c3c; font-weight: bold; }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="nav-content"><img src="/images/logo.jpg" alt="CohabiSafe Logo" class="logo"></div>
    </nav>

    <main class="container">
        <h1>What are your non-negotiables?</h1>
        <p>Select your preference level for each amenity.</p>

        <form action="/preferences/routine" method="GET"> <!-- Temporary GET for flow -->
            <div class="amenity-grid">
                <!-- Item 1 -->
                <div class="amenity-card">
                    <label>In-Unit Washer/Dryer</label>
                    <select class="preference-select" name="washer">
                        <option value="nice">Would Like</option>
                        <option value="must">Must Have</option>
                        <option value="no">Absolutely Not</option>
                        <option value="dc">Don't Care</option>
                    </select>
                </div>
                
                <!-- Item 2 -->
                <div class="amenity-card">
                    <label>Private Bathroom</label>
                    <select class="preference-select" name="bath">
                        <option value="nice">Would Like</option>
                        <option value="must">Must Have</option>
                        <option value="no">Absolutely Not</option>
                        <option value="dc">Don't Care</option>
                    </select>
                </div>

                <!-- Item 3 -->
                <div class="amenity-card">
                    <label>Pet Friendly</label>
                    <select class="preference-select" name="pets">
                        <option value="must">Must Have</option>
                        <option value="no">Absolutely Not</option>
                        <option value="nice">Would Like</option>
                    </select>
                </div>
                
                 <!-- Item 4 -->
                <div class="amenity-card">
                    <label>Parking Spot</label>
                    <select class="preference-select" name="parking">
                        <option value="nice">Would Like</option>
                        <option value="must">Must Have</option>
                        <option value="dc">Don't Care</option>
                    </select>
                </div>
            </div>

            <div class="quiz-nav">
                <button type="submit" class="btn-primary">Next: Routine Widget</button>
            </div>
        </form>
    </main>
    
    <script>
        // Color coding for selects
        document.querySelectorAll('select').forEach(sel => {
            sel.addEventListener('change', (e) => {
                e.target.className = 'preference-select ' + e.target.value;
            });
        });
    </script>
</body>
</html>